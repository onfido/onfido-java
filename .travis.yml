language: java
cache:
  directories:
    - "$HOME/.cache"

deploy:
  provider: script
  script: "cd onfido-java &&
           cp .travis.settings.xml $HOME/.m2/settings.xml &&
           mvn deploy"
  skip_cleanup: true
  on:
    branch: master
    tags: true
