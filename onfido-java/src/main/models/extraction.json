{
  "title": "Extraction",
  "path": "extractions",
  "properties": {
    "document_id": {
      "type": "string",
      "description": "The unique identifier of the uploaded document to run extraction on"
    },
    "document_classification": {
      "$ref": "document_classification.yaml",
      "description": "The document classification",
      "readOnly": true
    },
    "extracted_data": {
      "$ref": "extracted_data.yaml",
      "readOnly": true,
      "description": "As extraction is done on a best-effort basis, and also information varies across different documents, none of these properties are guaranteed be present in the response."
    }
  }
}