/*
 * Onfido Public API v3.6
 * The Onfido Public API (v3.6)
 *
 * The version of the OpenAPI document: v3.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.onfido.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.onfido.model.IdrSsnBreakdownBreakdownFullMatch;
import com.onfido.model.IdrSsnBreakdownBreakdownLast4DigitsMatch;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.onfido.JSON;

/**
 * IdrSsnBreakdownBreakdown
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.11.0")
public class IdrSsnBreakdownBreakdown {
  public static final String SERIALIZED_NAME_LAST4_DIGITS_MATCH = "last_4_digits_match";
  @SerializedName(SERIALIZED_NAME_LAST4_DIGITS_MATCH)
  @javax.annotation.Nullable
  private IdrSsnBreakdownBreakdownLast4DigitsMatch last4DigitsMatch;

  public static final String SERIALIZED_NAME_FULL_MATCH = "full_match";
  @SerializedName(SERIALIZED_NAME_FULL_MATCH)
  @javax.annotation.Nullable
  private IdrSsnBreakdownBreakdownFullMatch fullMatch;

  public IdrSsnBreakdownBreakdown() {
  }

  public IdrSsnBreakdownBreakdown last4DigitsMatch(@javax.annotation.Nullable IdrSsnBreakdownBreakdownLast4DigitsMatch last4DigitsMatch) {
    this.last4DigitsMatch = last4DigitsMatch;
    return this;
  }

  /**
   * Get last4DigitsMatch
   * @return last4DigitsMatch
   */
  @javax.annotation.Nullable
  public IdrSsnBreakdownBreakdownLast4DigitsMatch getLast4DigitsMatch() {
    return last4DigitsMatch;
  }

  public void setLast4DigitsMatch(@javax.annotation.Nullable IdrSsnBreakdownBreakdownLast4DigitsMatch last4DigitsMatch) {
    this.last4DigitsMatch = last4DigitsMatch;
  }


  public IdrSsnBreakdownBreakdown fullMatch(@javax.annotation.Nullable IdrSsnBreakdownBreakdownFullMatch fullMatch) {
    this.fullMatch = fullMatch;
    return this;
  }

  /**
   * Get fullMatch
   * @return fullMatch
   */
  @javax.annotation.Nullable
  public IdrSsnBreakdownBreakdownFullMatch getFullMatch() {
    return fullMatch;
  }

  public void setFullMatch(@javax.annotation.Nullable IdrSsnBreakdownBreakdownFullMatch fullMatch) {
    this.fullMatch = fullMatch;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the IdrSsnBreakdownBreakdown instance itself
   */
  public IdrSsnBreakdownBreakdown putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    IdrSsnBreakdownBreakdown idrSsnBreakdownBreakdown = (IdrSsnBreakdownBreakdown) o;
    return Objects.equals(this.last4DigitsMatch, idrSsnBreakdownBreakdown.last4DigitsMatch) &&
        Objects.equals(this.fullMatch, idrSsnBreakdownBreakdown.fullMatch)&&
        Objects.equals(this.additionalProperties, idrSsnBreakdownBreakdown.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(last4DigitsMatch, fullMatch, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class IdrSsnBreakdownBreakdown {\n");
    sb.append("    last4DigitsMatch: ").append(toIndentedString(last4DigitsMatch)).append("\n");
    sb.append("    fullMatch: ").append(toIndentedString(fullMatch)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("last_4_digits_match");
    openapiFields.add("full_match");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to IdrSsnBreakdownBreakdown
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!IdrSsnBreakdownBreakdown.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in IdrSsnBreakdownBreakdown is not found in the empty JSON string", IdrSsnBreakdownBreakdown.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the optional field `last_4_digits_match`
      if (jsonObj.get("last_4_digits_match") != null && !jsonObj.get("last_4_digits_match").isJsonNull()) {
        IdrSsnBreakdownBreakdownLast4DigitsMatch.validateJsonElement(jsonObj.get("last_4_digits_match"));
      }
      // validate the optional field `full_match`
      if (jsonObj.get("full_match") != null && !jsonObj.get("full_match").isJsonNull()) {
        IdrSsnBreakdownBreakdownFullMatch.validateJsonElement(jsonObj.get("full_match"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!IdrSsnBreakdownBreakdown.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'IdrSsnBreakdownBreakdown' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<IdrSsnBreakdownBreakdown> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(IdrSsnBreakdownBreakdown.class));

       return (TypeAdapter<T>) new TypeAdapter<IdrSsnBreakdownBreakdown>() {
           @Override
           public void write(JsonWriter out, IdrSsnBreakdownBreakdown value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else if (jsonElement.isJsonObject()) { 
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public IdrSsnBreakdownBreakdown read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             IdrSsnBreakdownBreakdown instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of IdrSsnBreakdownBreakdown given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of IdrSsnBreakdownBreakdown
   * @throws IOException if the JSON string is invalid with respect to IdrSsnBreakdownBreakdown
   */
  public static IdrSsnBreakdownBreakdown fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, IdrSsnBreakdownBreakdown.class);
  }

  /**
   * Convert an instance of IdrSsnBreakdownBreakdown to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

